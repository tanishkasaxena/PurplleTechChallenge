# Docker Plan

Planned Docker files:

```text
docker/
  Dockerfile.api
  Dockerfile.dashboard
```

The API container should run FastAPI on port `8000`.

The dashboard container should run Streamlit on port `8501`.

Docker implementation should be added after the API and dashboard modules exist.

